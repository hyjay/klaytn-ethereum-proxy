# Klaytn Ethereum Proxy
A JSON-RPC proxy server compatible with Ethereum clients for Klaytn.

**This is NOT for production.**

## Getting started

1. Install dependencies:

`$ yarn`

2. Start the server:

`$ yarn start`
